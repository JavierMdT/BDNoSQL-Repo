[project]
name = "bdnosql-repo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "redis>=7.0.0",
    "toml>=0.10.2",
    "tomli>=2.3.0",
]
